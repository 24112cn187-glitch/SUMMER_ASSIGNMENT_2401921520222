class Solution {
public:
    vector<int> sortedSquares(vector<int>& nums) {
       vector<int> v;
       int n=nums.size();
       for(int i=0;i<n;i++)
       {
        int a=nums[i]*nums[i];
        v.push_back(a);
       } 

       sort(v.begin() , v.end());
       return v;
        
    }
};
